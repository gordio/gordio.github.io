title: Oleg Gordienko – Yet Another Developer

markdown: kramdown
kramdown:
    # smart_quotes: ["rdquo", "rsquo", "ldquo", "rdquo"]
    smart_quotes:  ["apos", "apos", "quot", "quot"]
    input: GFM

defaults:
    -
        scope:
            path: "" # an empty string - all files in the project
            type: "posts"
        values:
            layout: "post"
    -
        scope:
            path: "" # an empty string - all files in the project
        values:
            layout: "default"

destination: .site
sass:
    style: :compressed
exclude:
    - CNAME
    - Gemfile
    - Gemfile.lock
    - Guardfile